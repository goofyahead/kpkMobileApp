<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/content_frame"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">
        <FrameLayout
            android:id="@+id/fragment_holder"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_weight="1"
            />
    </RelativeLayout>

    <!-- The navigation drawer -->
    <RelativeLayout
        android:clickable="true"
        android:background="@color/nowfie_gray"
        android:id="@+id/left_drawer"
        android:layout_gravity="start"
        android:layout_width="210dp"
        android:layout_height="match_parent">

        <ListView
            android:id="@+id/listView_categories"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:choiceMode="singleChoice"
            android:divider="@color/nowfie_gray"
            android:dividerHeight="1dp"
            android:layout_above="@+id/your_profile_title"
            />

        <TextView
            android:layout_centerHorizontal="true"
            android:textColor="@color/white"
            android:layout_above="@+id/profile_pic"
            android:id="@+id/your_profile_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/your_profile" />

        <ImageView
            android:id="@+id/profile_pic"
            android:layout_centerHorizontal="true"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:layout_above="@+id/listView_my_options"
            android:layout_marginBottom="6dp"/>

        <ListView
            android:layout_alignParentBottom="true"
            android:id="@+id/listView_my_options"
            android:background="@color/nowfie_light_purple"
            android:layout_width="match_parent"
            android:layout_height="110dp"
            android:choiceMode="singleChoice"
            android:divider="@color/nowfie_gray"
            android:dividerHeight="1dp"
            />

    </RelativeLayout>

</android.support.v4.widget.DrawerLayout>

